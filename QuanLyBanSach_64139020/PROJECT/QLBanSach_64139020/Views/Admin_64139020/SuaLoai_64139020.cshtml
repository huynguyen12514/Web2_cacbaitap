@model QLBanSach_64139020.Models.LOAISACH

@{
    ViewBag.Title = "Sửa Loại Sách";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<h2>Sửa Loại Sách</h2>

@using (Html.BeginForm("SuaLoai_64139020", "Admin_64139020", FormMethod.Post))
{
    @Html.AntiForgeryToken()
    @Html.HiddenFor(model => model.MaLoai)

    <div class="form-group">
        @Html.LabelFor(model => model.TenLoai, "Tên Loại Sách")
        @Html.TextBoxFor(model => model.TenLoai, new { @class = "form-control", placeholder = "Nhập tên loại sách" })
        @Html.ValidationMessageFor(model => model.TenLoai, "", new { @class = "text-danger" })
    </div>

    <div class="form-group mt-4">
        <div class="col-md-offset-2 col-md-10">
            <input type="submit" value="Lưu thay đổi" class="btn btn-primary rounded-pill px-4 " />
        </div>
    </div>
}

<div class="mt-3">
    @Html.ActionLink("← Trở lại", "LoaiSach_64139020", null, new { @class = "btn btn-outline-secondary rounded-pill px-3" })
</div>